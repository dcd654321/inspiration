# 灵感记录开发约定

## 项目定位

原生微信小程序「灵感记录」。用户随口记下灵感，后续可补充、拍照附图，由 AI 扩展成更完整的想法，并提炼出该灵感的热度信号。技术栈与 `yidian-miniprogram` 一致：JavaScript / WXML / WXSS + 微信云开发（云函数 + 云数据库 + 云存储 + 云开发 AI）。

当前状态：**仅完成工程初始化骨架，业务功能尚未实现**。云环境和 AI 能力均默认关闭。

## 分支与授权

- 所有开发、文档、测试及提交在 `dev` 分支进行，开始前检查工作区并同步 `origin/dev`，不得覆盖用户未提交修改。
- 只有用户测试确认并明确要求合并时，才能将 `dev` 合并到 `main`；不得自行向 `main` 提交或推送，不强制推送。
- 需求确认、代码实现、云端部署、AI 启用、测试验收、合并发布是不同动作；一次授权不自动涵盖后续阶段。

## OpenSpec

- 使用锁定版本的 `@fission-ai/openspec` 开发依赖，需 Node.js >=20.19.0。运行 `npm run openspec -- <command>`；不得打包到小程序或云函数运行依赖。
- 新功能、数据同步、身份认证、数据库、AI 能力和部署变更先提案，再按用户授权实施。纯文案、样式微调及不改变行为的小修复可直接处理并留记录。
- 正文用中文，保留 OpenSpec 结构标题和 SHALL/MUST。先核验代码再写现状；未验证行为不得写成已实现事实。
- 用户主要审阅范围、非目标、验收标准；不要求用户撰写整套文档，不重抄历史 PRD。
- 任务只有取得对应验证证据才勾选；规范校验、自动测试、云部署、真机和多账户验收分别报告。
- 归档不等于 Git 合并或正式发布，不得跳过用户测试确认。

## 数据与共享环境安全

- 云端是正式账户数据来源，本地仅缓存已确认快照和待同步操作。保留离线队列、幂等、冲突及账户隔离约束。
- 新资源使用 `linggan_` 前缀。其他小程序函数、集合和共用认证配置不在默认修改范围。
- 身份只来自受信平台上下文（微信云函数上下文），不使用客户端传入的 AppID/OpenID 认证，不为接通而放宽权限。
- 数据迁移、清空、删除、正式部署及共用资源修改，执行前说明精确目标、影响及恢复边界，并取得适用授权。
- 密钥、令牌、账户身份样本、原始生产数据及本机配置不进 Git。变更保留差异、验证结果及回滚说明。
- 拍照上传的图片属于用户个人内容，不得写入日志、不得用于训练或分享，删除灵感时必须同步清理云存储文件。

## AI 能力

- AI 扩展与热度提炼走云开发 AI 能力，模型密钥只放云函数侧，前端包内不得出现任何密钥。
- AI 输出必须先通过 `miniprogram/core/ai-contract.js` 的结构校验和内容安全检查才可展示；校验失败时降级为提示用户重试，不得写入用户数据。
- AI 是增强而非依赖：AI 不可用时，记录、补充、拍照、列表浏览必须完全可用。
- 不得把规则拼接的本地结果宣传为 AI 生成；不得宣称未验证的模型效果。
- 额度、限流、内容安全和失败降级须在启用前完成，未完成时保持 `miniprogram/config/ai.js` 的 `enabled: false`。

## 验证

- 常规：`npm test`、`npm run check`。
- 规范：`npm run openspec -- validate --all --strict`。
- `npm run check` 只做语法、配置与页面文件的结构检查；WXML/WXSS 真实编译和渲染必须在微信开发者工具中确认，模拟器状态不等同于真机验收。
- 云函数部署、AI 真实调用、多账户隔离与真机验收分别留证据，不得合并报告。
