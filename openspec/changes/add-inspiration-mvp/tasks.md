# Tasks

> 全部任务均未开始。复选框只在取得对应验证证据后才可勾选；实施、自动测试、云端验证与用户验收分别列项，不得合并勾选。

## 1. 领域模型与校验

- [ ] 1.1 实现 `miniprogram/core/inspiration.js`：灵感、补充、图片记录的创建与校验，含正文长度上限、空内容拒绝、`id` 必填。验证：`npm test` 中新增用例覆盖空内容、超长正文、缺 `id` 三种输入。
- [ ] 1.2 实现原始正文不可变约束：提供追加补充与追加图片的纯函数，不提供改写 `text` 的路径。验证：单元测试断言追加后 `text` 与初始值全等。
- [ ] 1.3 实现 `miniprogram/core/heat.js`：五类信号的归一化、加权、0—100 取整，以及由信号直接生成理由。验证：单元测试断言同一输入重复计算结果相同、列表与详情取值一致。
- [ ] 1.4 实现 `miniprogram/core/ai-contract.js`：AI 输出结构、类型、长度校验与越界内容拒绝。验证：单元测试覆盖缺字段、类型错误、越界建议、命中安全规则四类输入，均须抛错。

## 2. 本机状态与持久化

- [ ] 2.1 实现 `miniprogram/services/store.js`：账户级快照、待同步队列、幂等键。验证：单元测试断言同一 `id` 重复入队只产生一次效果。
- [ ] 2.2 实现 `miniprogram/services/capture-drafts.js`：未保存补充的会话内草稿，避免切页丢失。验证：单元测试断言切换页面后草稿保留、结束会话后不承诺保留。
- [ ] 2.3 实现保存失败与离线路径：失败时保留输入、明确提示未保存成功、不报告虚假成功。验证：单元测试用注入的失败响应断言提示文案与输入保留。

## 3. 页面实现

- [ ] 3.1 实现 `pages/capture`：单输入框记录 + 保存，冷启动到落记录一次输入一次点击。验证：开发者工具中手动走通，记录耗时与操作次数。
- [ ] 3.2 实现 `pages/detail`：正文、补充时间线、照片区、热度区与 AI 扩展入口。验证：开发者工具中手动走通展示与追加补充。
- [ ] 3.3 实现 `pages/list`：按 `updatedAt` 倒序，最近补充排前。验证：开发者工具中用多条不同时间数据核对排序。
- [ ] 3.4 实现 `pages/mine`：数据说明、隐私说明（含照片用途）、反馈入口。验证：开发者工具中逐项打开确认文案完整。
- [ ] 3.5 注册页面与配置：更新 `app.json`、`config/cloud-resources.js`，保持 `config/cloud.js` 与 `config/ai.js` 的 `enabled` 为 `false`。验证：`npm run check` 通过且配置值仍为 `false`。

## 4. 拍照上传

- [ ] 4.1 实现权限申请与拒绝分支：相机、相册权限未授权时不读取任何图片，给出开启指引。验证：开发者工具中模拟拒绝权限，断言无图片读取且文字功能可用。
- [ ] 4.2 实现压缩、单张大小与单条数量上限校验。验证：单元测试覆盖压缩后超限、超出数量上限两种输入。
- [ ] 4.3 实现上传幂等：以 `photoId` 作为存储对象名，重传覆盖同一对象。验证：单元测试断言重复提交后图片记录数与对象数均为 1。
- [ ] 4.4 实现删除联动：灵感删除取得云端确认后清理图片记录与云存储文件，确认失败则全部保留。验证：单元测试覆盖确认成功与确认失败两条路径。

## 5. AI 能力

- [ ] 5.1 实现 `cloudfunctions/linggan_ai`：仅服务端调用模型，密钥取自环境变量，输出经契约校验与内容安全过滤。验证：本地对函数处理逻辑做单元测试；密钥不出现在仓库与前端包的检查纳入 `npm run check` 输出。
- [ ] 5.2 实现账户维度与会话维度双重额度限制，超限返回明确状态码。验证：单元测试覆盖额度耗尽分支。
- [ ] 5.3 实现前端确认流程：草案可逐条采纳/修改/放弃，确认前不写入，离开页面丢弃半成品。验证：单元测试断言未确认时灵感数据无变化。
- [ ] 5.4 实现全部降级路径：未启用、超时、额度耗尽、校验失败均提示且不影响基础功能。验证：单元测试逐一注入四种状态并断言基础功能仍返回成功。

## 6. 云端与协议

- [ ] 6.1 实现 `cloudfunctions/linggan_api`：可信上下文取身份，拒绝客户端传入身份字段，按 `(accountKey, id)` 幂等写入。验证：单元测试覆盖伪造身份字段被拒的场景。
- [ ] 6.2 实现 `server/` 协议与仓库层，与云函数入口分离以便构建产物一致。验证：单元测试 + 构建产物一致性检查。
- [ ] 6.3 实现 `generation` 代际递增与删除回执：删除后旧设备离线队列不得回传。验证：单元测试断言代际变更后旧队列写入被拒。
- [ ] 6.4 实现云存储访问控制与路径隔离：`linggan/{accountKey}/{inspirationId}/{photoId}`。验证：单元测试覆盖越权拼接路径被拒。

## 7. 自动化验证

- [ ] 7.1 `npm test` 全部通过，输出用例总数。验证：命令输出留档。
- [ ] 7.2 `npm run check` 全部通过：JSON 可解析、JS/CJS 可编译、`app.json` 页面文件齐全、tabBar 指向有效页面。验证：命令输出留档。
- [ ] 7.3 `npm run openspec -- validate --all --strict` 通过。验证：命令输出留档。
- [ ] 7.4 前后端密钥泄露检查：全仓检索模型密钥与厂商凭据，前端包内结果为空。验证：检索命令与输出留档。

## 8. 云端验证（需独立授权）

- [ ] 8.1 取得授权后创建集合 `linggan_accounts` 并配置权限规则。验证：后台截图与规则文本留档。
- [ ] 8.2 取得授权后部署 `linggan_api` 与 `linggan_ai`，配置环境变量。验证：部署记录与函数列表留档。
- [ ] 8.3 两个微信账户交叉验证：各自只能读写自己的灵感，伪造身份字段被拒。验证：双账户操作记录与返回码留档。
- [ ] 8.4 删除闭环验证：删除带照片的灵感后，云端记录与存储文件均已清理。验证：云存储文件列表前后对比留档。

## 9. 用户验收（需真机）

- [ ] 9.1 iOS 与 Android 真机走通「记录 → 补充 → 拍照 → AI 扩展 → 查看热度」全流程。验证：真机录屏或截图留档。
- [ ] 9.2 权限拒绝、断网上传、AI 超时三类失败场景在真机复现并确认降级正常。验证：真机操作记录留档。
- [ ] 9.3 窄屏与大字号下页面无遮挡、无截断。验证：真机截图留档。
- [ ] 9.4 用户确认验收结果。验证：用户明确回复。在此之前不得归档该变更，也不得合并到 `main`。
