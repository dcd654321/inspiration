# 规范覆盖率

**这份表不是统计，是闸门。** `tests/spec-coverage.test.cjs` 会校验它：规范里新增一条场景而这张表没跟着更新，`npm test` 直接失败。

没有这道闸门时发生过什么：规范里写着 83 条场景，仓库里没有任何东西能告诉你哪条有实现、哪条没有。加了新场景没人管，删了旧实现也没人知道。

## 状态含义

| 状态 | 含义 |
| --- | --- |
| **已覆盖** | 有自动化测试断言了这条行为，测试名写在第四列 |
| **部分覆盖** | 有测试，但只覆盖了其中一部分（另一半在别的层，第四列写明缺哪一段） |
| **待验收** | **已实现，但只有人能验**——WXML/WXSS 渲染、真机交互，Node 里测不了。第四列写明要在哪验 |
| **待实现** | 还没有对应实现，第四列写清属于哪一层 |

第三列写「已覆盖」或「部分覆盖」时，第四列**必须以「测试名」开头**，名字要与 `tests/*.test.cjs` 里的完全一致——闸门会去核对它真的存在。

**「待验收」不是「已覆盖」的委婉说法。** 页面写完不等于验过：原型是 HTML、真实页面是 WXML，两者之间没有任何自动比对（`detailed-design.md` §0 已写明闸门管不到这件事）。在开发者工具里走通、并在 `VERIFICATION.md` 留下证据之前，这一条就只能停在「待验收」。

## 排除范围

`inspiration-heat` 整份不在本变更内（已暂缓，见 `proposal.md`），不纳入本表。它的 11 条场景等后续独立变更立项时再纳入。

---

## inspiration-capture

| 要求 | 场景 | 状态 | 测试用例 / 说明 |
| --- | --- | --- | --- |
| 一句话快速记录 | 只输入正文即可保存 | 已覆盖 | 「只输入正文即可创建，其余字段有确定初值」 |
| 一句话快速记录 | 空内容不产生记录 | 已覆盖 | 「空内容不产生记录，纯空白同样视为空」 |
| 一句话快速记录 | 正文长度超限 | 已覆盖 | 「正文超出上限被拒绝，且不静默截断」 |
| 原文可直接编辑并保留历史 | 直接修改原文 | 已覆盖 | 「改写原文后旧版本进入历史」 |
| 原文可直接编辑并保留历史 | 回看历史版本 | 部分覆盖 | 「连续多次改写不丢早期版本」；历史的完整性与顺序已覆盖，**历史版本页已实现**，但 WXML 渲染与跳转需在开发者工具中确认 |
| 原文可直接编辑并保留历史 | 修改为空 | 已覆盖 | 「改写为空或超长被拒绝，且历史不变」 |
| 原文可直接编辑并保留历史 | 改写原文不影响补充 | 已覆盖 | 「改写原文不影响已有的补充」 |
| 原文可直接编辑并保留历史 | 反复改写不丢早期版本 | 已覆盖 | 「连续多次改写不丢早期版本」 |
| 单条补充的直接操作 | 打开操作入口 | 待验收 | 长按补充弹出面板（修改 / 合并进灵感 / 删除），已收起的补充只给恢复与删除；需在开发者工具中确认长按手势与面板呈现（task 3.6） |
| 单条补充的直接操作 | 修改补充 | 已覆盖 | 「修改补充：内容被替换，旧内容进这条补充自己的历史」 |
| 单条补充的直接操作 | 修改为空 | 已覆盖 | 「修改补充为空或超长被拒绝」 |
| 单条补充的直接操作 | 合并进灵感 | 已覆盖 | 「合并进灵感：内容追加到末尾，本条收起，原文旧版本进历史」 |
| 单条补充的直接操作 | 合并前的原文可回看 | 已覆盖 | 「合并进灵感：内容追加到末尾，本条收起，原文旧版本进历史」 |
| 单条补充的直接操作 | 合并后原文照常可编辑 | 已覆盖 | 「合并后原文仍可编辑，合并前的版本还在历史里」 |
| 单条补充的直接操作 | 恢复已并入灵感的补充 | 已覆盖 | 「恢复已并入的补充：重新出现在时间线，原文不回退」 |
| 单条补充的直接操作 | 删除补充前必须确认 | 待验收 | 点「删除」先落到确认弹窗，正文写明「此操作无法撤销」并**把待删内容放进带时间的框**；需在开发者工具中确认取消路径不改变任何数据（task 3.6） |
| 单条补充的直接操作 | 删除后从时间线移除 | 已覆盖 | 「删除补充：从时间线移除」 |
| 单条补充的直接操作 | 操作失败不留痕 | 已覆盖 | 「合并撑破正文上限时整条拒绝，不做截断」「汇总失败不留痕：失败后被选中的内容与汇总前一致」 |
| 已合并内容的标记与恢复 | 已合并的补充默认收起 | 部分覆盖 | 「覆盖：汇总结果写入，来源被标记为已合并」；标记逻辑与**收起行、展开、恢复**均已实现，但收起行只在存在已合并/已并入项时出现，WXML 渲染需在开发者工具中确认 |
| 已合并内容的标记与恢复 | 展开并恢复已合并的补充 | 已覆盖 | 「恢复一条已合并的补充：内容回来，汇总结果不受影响」 |
| 已合并内容的标记与恢复 | 已合并的灵感 | 部分覆盖 | 「灵感可被整条标记为已合并，也可恢复」；标记与恢复已覆盖，**列表中「已合并 N 条」入口未实现**（task 5A.4） |
| 已合并内容的标记与恢复 | 已合并的内容仍可被读取 | 已覆盖 | 「被覆盖的内容完整可读——只是收起，不是截断」 |
| 多次补充与时间线 | 追加多条补充 | 已覆盖 | 「多次补充按时间顺序完整保留」 |
| 多次补充与时间线 | 补充内容为空 | 已覆盖 | 「空补充不写入，也不改变更新时间」 |
| 未提交的补充不因切页丢失 | 切页后草稿仍在 | 已覆盖 | 「切页后草稿仍在——同一会话内共用一个实例」 |
| 未提交的补充不因切页丢失 | 提交后草稿清除 | 已覆盖 | 「提交后草稿清除」 |
| 未提交的补充不因切页丢失 | 会话结束后不做承诺 | 已覆盖 | 「会话结束后不承诺保留——新实例读不到旧草稿」 |
| 未提交的补充不因切页丢失 | 不同灵感的草稿互不干扰 | 已覆盖 | 「不同灵感的草稿互不干扰」 |
| 记录与补充的持久化 | 保存成功 | 已覆盖 | 「保存成功：本机写入，云端确认」 |
| 记录与补充的持久化 | 同步未成功 | 已覆盖 | 「同步未成功：内容已落本机，进队列等重试」 |
| 记录与补充的持久化 | 本机写入失败 | 已覆盖 | 「本机写入失败：报告失败，且不改动已有的数据」 |
| 记录与补充的持久化 | 界面不区分两层存储 | 已覆盖 | 「产品代码里不出现禁用措辞」——`tests/copy-rules.test.cjs` 机械保证产品代码里不出现「本机存储 / 云端存储 / 待同步」这类分层措辞 |
| 记录与补充的持久化 | 重复提交同一次保存 | 部分覆盖 | 「同一 id 重复入队只产生一次效果」「同一 id 重复保存只产生一条灵感」；**本机队列幂等已覆盖，服务端 `requestId` 传输层幂等未实现**（task 6.1） |
| 灵感列表与浏览 | 列表排序 | 已覆盖 | 「列表按更新时间倒序，最近有补充的排在前面」 |
| 灵感列表与浏览 | 打开详情 | 待验收 | 详情页与历史版本页已实现；需在开发者工具中确认渲染、「编辑原文 → 保存 → 历史」这条链路，以及长按面板未实现时的降级（task 3.2） |
| 账户数据隔离 | 两个用户独立使用 | 待实现 | 云端：集合权限规则与双账户验证（task 8.1、8.3） |
| 账户数据隔离 | 身份缺失或非法来源 | 待实现 | 云端：可信上下文取身份（task 6.1） |

## photo-capture

| 要求 | 场景 | 状态 | 测试用例 / 说明 |
| --- | --- | --- | --- |
| 拍照与相册选图 | 拍照附加 | 待验收 | `services/photo.js` 的选图与校验已实现，**详情页的照片区尚未接线**（task 4.5）——在没有云存储之前接上去只能显示一批刷新就消失的缩略图 |
| 拍照与相册选图 | 相册多选 | 待验收 | 用例「相册多选：全部收下并按选择顺序返回」；同上的接线缺口 |
| 拍照与相册选图 | 用户拒绝权限 | 已覆盖 | 「权限被拒时不读取任何图片，并给出开启指引」（断言 `chooseMedia` 一次都没被调用） |
| 图片压缩与限制 | 超出单张大小上限 | 已覆盖 | 「压缩后仍超限：拒绝该张，其余不受影响」「原图超限则压缩，压缩后合格就收下」 |
| 图片压缩与限制 | 超出数量上限 | 已覆盖 | 「图片数量达到上限后阻止继续添加」「已达数量上限时在读图之前就拦住」 |
| 上传失败可重试 | 网络中断后重试 | 已覆盖 | 「上传失败后重试能成功」「上传失败返回失败，调用方据此保留本地文件」 |
| 上传失败可重试 | 同一次上传被重复提交 | 已覆盖 | 「同一 photoId 重传覆盖同一对象，云端只留一个」（断言三次上传后对象数仍为 1） |
| 上传失败可重试 | 上传失败不影响文字 | 待实现 | 架构上保证（照片与文字是两条独立路径），但**页面尚未接线，无从验证**（task 4.5） |
| 图片与灵感同生命周期 | 删除灵感后清理文件 | 已覆盖 | 「删除：清理云存储后物理移除」 |
| 图片与灵感同生命周期 | 删除确认失败 | 已覆盖 | 「删除时清理失败则回滚，灵感与照片全部保留」（断言 `deletedAt` 被回滚、照片记录仍在） |
| 图片与灵感同生命周期 | 照片不可被他人访问 | 部分覆盖 | 「标识含越权字符的提交被拒绝」等路径隔离用例已覆盖；**云存储访问控制规则本身未配置**，那在云开发控制台，属 task 8.1 |
| 照片隐私边界 | 诊断日志不含照片 | 待实现 | 日志约束，需在上传真正接线时一并核对（task 4.5） |
| 照片隐私边界 | 无分享出口 | 待验收 | 界面上确实没有任何分享入口，但没有自动化测试守着，需在开发者工具中确认 |

## ai-expansion

| 要求 | 场景 | 状态 | 测试用例 / 说明 |
| --- | --- | --- | --- |
| 从灵感生成结构化草案 | 生成草案 | 已覆盖 | 「扩展成功返回通过契约校验的草案」。**模型调用是注入的**——厂商未定时返回 `AI_DISABLED`，绝不伪造结果 |
| 从灵感生成结构化草案 | 内容过短无法扩展 | 已覆盖 | 「正文过短不请求扩展，也不消耗额度」（断言模型一次都没被调用） |
| 从灵感生成结构化草案 | 草案不改变原灵感 | 待验收 | `services/ai.js` 结构上不接触灵感对象（只接收 text / items），但**没有专门用例断言这一点**；需在开发者工具中确认请求扩展后原文与补充确实不变 |
| 用户确认后才落盘 | 采纳部分内容 | 待实现 | 页面上的逐条采纳交互（task 5.3）。领域层的写入原语已就绪 |
| 用户确认后才落盘 | 放弃草案 | 待实现 | 页面上的放弃交互（task 5.3）。领域层不写数据，放弃不需要额外的清理逻辑——草案从来没进过数据 |
| 用户确认后才落盘 | 确认前离开 | 待实现 | 草案只存在于页面内存，离开即丢弃——页面未实现 |
| 输出校验与内容安全 | 输出结构不符合契约 | 已覆盖 | 「输出结构不符合契约时拒绝，并退还额度」；核心层的七条结构用例见 `ai-contract.test.cjs` |
| 输出校验与内容安全 | 输出命中内容安全规则 | 已覆盖 | 「命中医疗用药规则即拒绝」等三条越界用例；服务层「命中内容安全与结构不合法返回不同的错误码」 |
| 输出校验与内容安全 | 模型返回越界建议 | 已覆盖 | 「模型返回越界建议时按失败处理」 |
| AI 不可用时的降级 | AI 未启用 | 已覆盖 | 「AI 未启用时明确说明，不伪造任何结果」。**绝不返回一个看起来像 AI 的结果** |
| AI 不可用时的降级 | 超时或失败 | 已覆盖 | 「超时返回明确状态，且不影响后续调用」「模型抛错时退额度并返回失败」 |
| AI 不可用时的降级 | 额度耗尽 | 已覆盖 | 「额度耗尽返回明确状态，并说明是额度限制」「额度在调用之前扣，超额的那次不会真的调出去」 |
| 密钥与调用边界 | 前端包中无密钥 | 已覆盖 | 「小程序包与云函数中不含模型密钥」 |
| 密钥与调用边界 | 额度按账户计算 | 部分覆盖 | 「额度耗尽返回明确状态，并说明是额度限制」；**会话维度已覆盖**（注入式 quota），**账户维度未实现**——它需要服务端的计数器存储，等云端接入后补（task 5.2） |
| 不得夸大 AI 效果 | 结果来源标注 | 待实现 | 界面层：AI 标注与免责说明（task 5.3） |
| 不得夸大 AI 效果 | 未接入时的表现 | 待验收 | 详情页与补充分区都不展示任何看似 AI 产出的内容；需在开发者工具中确认 |

## ai-summarize

| 要求 | 场景 | 状态 | 测试用例 / 说明 |
| --- | --- | --- | --- |
| 把多条补充汇总成一条 | 汇总多条补充 | 已覆盖 | 「覆盖：汇总结果写入，来源被标记为已合并」；服务层「汇总成功返回单段文本」 |
| 把多条补充汇总成一条 | 只选了一条 | 已覆盖 | 「只选一条不能汇总」（领域层）＋「只选一条不发起汇总，也不消耗额度」（服务层） |
| 把多条补充汇总成一条 | 汇总不改动未选中的补充 | 已覆盖 | 「覆盖：汇总结果写入，来源被标记为已合并」（断言未选中的 sup_3 仍在时间线上） |
| 把多条补充汇总成一条 | 汇总结果的来源可追溯 | 已覆盖 | 「覆盖：汇总结果写入，来源被标记为已合并」（断言 `mergedInto` 指向汇总结果） |
| 把多个灵感汇总成一个 | 勾选多个灵感汇总 | 已覆盖 | 「覆盖：汇总结果成为目标的原文，旧原文进历史」「另存：汇总结果成为一条新灵感，来源全部收起」。**这是本轮补上的那条真实缺口**——此前领域层只有 `markMerged` / `unmerge` 没有编排函数 |
| 把多个灵感汇总成一个 | 只勾选了一个 | 已覆盖 | 「少于两个来源被拒绝」「只选一条不发起汇总，也不消耗额度」 |
| 把多个灵感汇总成一个 | 未勾选的灵感不受影响 | 已覆盖 | 「未勾选的灵感完全不受影响」 |
| 把多个灵感汇总成一个 | 汇总结果的来源可追溯 | 已覆盖 | 「覆盖：其余来源被标记为已合并，指向目标」「另存：汇总结果成为一条新灵感，来源全部收起」 |
| 覆盖与另存由用户选择 | 选择覆盖 | 已覆盖 | 「覆盖：汇总结果成为目标的原文，旧原文进历史」 |
| 覆盖与另存由用户选择 | 选择另存 | 已覆盖 | 「另存：汇总结果成为一条新灵感，来源全部收起」 |
| 覆盖与另存由用户选择 | 覆盖后恢复 | 已覆盖 | 「恢复一条已合并的补充：内容回来，汇总结果不受影响」 |
| 覆盖与另存由用户选择 | 覆盖不产生数据丢失 | 已覆盖 | 「被覆盖的内容完整可读——只是收起，不是截断」 |
| 确认后才写入 | 放弃汇总 | 待实现 | 页面上的汇总草案面板（task 5A.2）。领域层不写数据，写入是独立的一次调用 |
| 确认后才写入 | 确认前离开 | 待实现 | 草案只存页面内存，离开即丢弃——页面未实现 |
| 汇总结果的输出校验与内容安全 | 输出不符合契约 | 已覆盖 | 「汇总输出为空时按契约失败处理」「汇总结果为空、缺字段或类型不对都被拒绝」 |
| 汇总结果的输出校验与内容安全 | 输出命中内容安全规则 | 已覆盖 | 「汇总结果同样过内容安全，与扩展草案共用同一套规则」 |
| 汇总失败与 AI 不可用时的降级 | AI 未启用 | 已覆盖 | 「AI 未启用时汇总也不可用」 |
| 汇总失败与 AI 不可用时的降级 | 汇总失败 | 已覆盖 | 「模型抛错时退额度并返回失败」「超时返回明确状态，且不影响后续调用」 |
| 汇总失败与 AI 不可用时的降级 | 失败不留痕 | 已覆盖 | 「失败不留痕：被拒绝时原数组一个字节都没变」 |
| 不得夸大汇总效果 | 结果来源标注 | 待实现 | 界面层：汇总结果的 AI 标注与免责说明（task 5A.2） |
| 不得夸大汇总效果 | 未接入时的表现 | 待验收 | 未接入时不展示任何看似 AI 的汇总结果；需在开发者工具中确认 |

---

## 这张表现在告诉你什么

- **87 条场景**：已覆盖 61、部分覆盖 6、待验收 9、待实现 11。
- 待实现的 11 条**集中在页面的交互层**（AI 草案的采纳/放弃、汇总的选择态与预览、照片区的接线），云端与领域层已经没有空的。
- **两条真实缺口，一条已补、一条仍缺**：
  - ✅ 「勾选多个灵感汇总」——**已补**。新增 `core/merge.js` 的 `mergeInspirations`，14 项用例覆盖覆盖与另存两种模式。
  - ⏳ 「额度的账户维度」——仍缺。会话维度已实现（注入式 quota），账户维度需要服务端的计数器存储，等云端接入后补。
- **9 条「待验收」**。页面写完不等于验过：原型是 HTML、真实页面是 WXML，两者之间没有任何自动比对。在开发者工具里走通并留下证据之前，它们只能停在「待验收」——**不是「已覆盖」的委婉说法**。
- 部分覆盖的 6 条都标明了缺哪一段，不会含糊过去。
- **一处规范补写**：任务 2.2 的「未提交补充的会话内草稿」原本在规范里没有对应场景——**是任务清单里有、规范里没有**。已补进 `inspiration-capture`（「未提交的补充不因切页丢失」）。这类缺口与上面那条方向相反，但同样是跑偏。

## 维护规则

1. **规范里加一条场景，这张表必须同步加一行**，否则 `npm test` 失败。
2. **实现一条场景**：领域层/服务层的改成「已覆盖」并写上测试名；只有人能验的改成「待验收」并写清在哪验。
3. **删掉一条场景，这张表里的行也要删**——陈旧条目同样会让测试失败。
4. 界面的场景在 `docs/ui-mockup.html` 里有原型，但**原型不等于验证**。页面写完了也仍停在「待验收」，直到在开发者工具里走通、并在 `VERIFICATION.md` 留下证据。
5. **「待验收」不能当「已覆盖」用。** 它存在的意义就是把「写完了」和「验过了」分开——两者混在一起，这张表就回到了它诞生之前的状态。
