# 界面设计

> 状态（2026-09-22）：本文档描述 `add-inspiration-mvp` 四个页面的区块划分、元素构成与状态呈现。
> 本文档只解决「页面长什么样、空态/加载态/失败态各显示什么」，不重复描述需求与验收标准。
> **热度提炼已暂缓**，四个页面均不含热度展示位。WXML/WXSS 的真实编译与渲染必须在微信开发者工具中确认，本文档不代替真机验收。

## 1. 全局约定

**样式令牌**（沿用 `miniprogram/app.wxss`，不新增令牌）

**样式令牌**（2026-09-22 修订。括号内为 750rpx 设计稿取值，1px = 2rpx）

| 用途 | 值 |
| --- | --- |
| 正文与标题 | `#16161d` |
| 次要文字 | `#5f5f6e` |
| 最淡（时间戳、占位符） | `#9b9baa` |
| 页面底色 | `#f5f5f8` |
| 承载面 | `#ffffff` |
| 分隔线 | `#e8e8ee`；重分隔线 `#d8d8e2` |
| 主色（按钮、选中态、AI 标记） | `#3d3d8f`；浅底 `#ececf7`；描边 `#c9c9e6` |
| 错误 | `#b3382c`；浅底 `#fbeeec`；描边 `#efc9c4` |
| 警示 | `#8a6420`；浅底 `#fdf4e3` |
| 圆角 | 小 `16rpx` / 中 `24rpx` / 大 `32rpx` |
| 页面左右留白 | `40rpx` |
| 触摸目标 | 不小于 `88rpx` |
| 按钮高度 | 常规 `80rpx`，小号 `68rpx` |

**⚠️ 与现状的差异**：`miniprogram/app.wxss` 目前仍是旧值（底色 `#f7f7fa`、正文 `#1f1f27`、次要 `#6b6b76`、更淡 `#a8a8b3`）。实施阶段需要把上表写入 `app.wxss`，否则页面会与原型不一致。**本文件描述的是目标值，不是当前值。**

**视觉原则**（决定了上面这些值怎么用）

1. **原文是锚点。** 详情页的原始正文**不放进卡片**，直接落在页面底色上，用全页最大的字号（`38rpx` / 行高 1.78）。其余所有内容都从属于它。
2. **层级用留白和分隔线做，不用阴影。** 列表是连续承载面加发丝分隔线，不是一张张浮起的卡片。全页只有手机外框带阴影（那是原型的容器，不是产品的一部分）。
3. **草案是临时的。** AI 草案用虚线描边、浅底、并显式标注「尚未写入」。这不是装饰——它把规范里「确认前不得写入用户数据」这条规则变成了看得见的信息。
4. **同步状态只在异常时出现。** 正常同步的条目不带任何标记，避免每一条都挂一个无意义的徽标。
5. **承载面用在真正需要「一整块内容」的地方**（列表、能力状态、隐私说明、草案），不滥用卡片把页面切成一格一格。

**可视化原型**：`docs/ui-mockup.html`，按本文件的区块划分与上面的令牌绘制的 14 个界面（含三种状态、本机写入失败、列表选择态、原文历史、长按补充的操作面板与删除确认、汇总草案的写入方式选择、以及 AI 未启用时的降级形态）。原型只用于对齐布局与文案，**不是可运行的小程序**，WXML/WXSS 的真实编译与渲染仍需在微信开发者工具中确认。

**提示方式**：分两类，不能混用。

- **页面内联提示**——用于用户会据此误判数据状态的场景（保存失败、同步失败、删除失败、上传失败）。`wx.showToast` 会自动消失，用户错过之后会以为已经成功，所以这类提示必须常驻在页面上，直到状态改变。
- **轻提示 `wx.showToast`**——仅用于已确定成功、无需用户处理的正向反馈（如「已保存」）。

**三态覆盖**：每个页面、每个异步数据区都必须写出**空态、加载态、失败态**三种呈现。骨架版本里只写了正常态的占位，这是 AI 实现时最容易漏的部分。

**实现约定**：

- 页面 `.wxss` 只放该页独有样式，通用样式一律加进 `app.wxss`。
- 所有可点击元素的 `disabled` 状态必须有视觉区分，不能只靠逻辑拦截。
- 按钮文案在异步过程中要改变（如「保存」→「保存中…」），不能静默等待。

**导航栏标题**（2026-09-22 补。此前没规定过，结果原型与代码各写各的，分叉了一次）：

| 页面 | 导航栏标题 | 理由 |
| --- | --- | --- |
| 三个 tab 页（记录 / 灵感 / 我的） | **小程序名「灵感拾光簿」** | tab 栏已经标了区块名，导航栏再重复一遍是冗余。改用小程序名，让人随时能确认打开的是哪个小程序；页面功能由**页内标题**承担（「记下这一句」「我的灵感」「我的」） |
| 二级页（灵感详情 / 历史版本） | 功能名 | 它们没有 tab 标签，导航栏必须回答「我在哪」 |

**排查「标题没变」时先看页面自己的 json**：`app.json` 的 `window.navigationBarTitleText` 只是**全局默认值**，任何页面在 `pages/*/index.json` 里设了自己的标题就会把它盖掉。改全局值不影响这些页面——这是这次踩到的坑。

## 2. 记录页 `pages/capture`

Tab 首页，冷启动默认落地页。设计目标：**一次输入 + 一次点击完成记录**。

### 区块

| 区块 | 内容 |
| --- | --- |
| A 标题区 | 标题「记下这一句」，副标题「半句话也可以。」——不罗列功能，只给用户一个「写不完整也没关系」的许可 |
| B 输入卡片 | `textarea`（`maxlength` 取 `LIMITS.textMaxLength`，`auto-height`）+ 右下角字数计数器 + 全宽主按钮「保存」 |

**移除**骨架版本中的 `.placeholder` 说明块——它属于工程说明，不应出现在产品界面。

### 状态

| 状态 | 呈现 |
| --- | --- |
| 空态 | 占位符「此刻想到什么？」。计数器显示 `0 / 2000`。保存按钮 **disabled**（置灰） |
| 输入中 | 计数器实时更新。超过上限 90% 时计数器转为警示色 |
| 保存中 | 按钮文案「保存中…」且 disabled，`textarea` 设为只读 |
| 保存成功 | 清空输入，计数器归零，按钮恢复「保存」并回到 disabled。轻提示「已保存」 |
| 同步未成功 | **输入照常清空**——内容已经落在本机，不会丢，留着反而会让用户重复提交。页面内联提示「已保存。还没同步到云端，会自动重试。」 |
| 写入失败 | **输入原样保留**。这是唯一需要保留输入的情况——真的没存下来。内联提示「存储空间不够了，这条没能存下来。」**不得静默失败** |

**区分「同步未成功」与「写入失败」是刻意的。** 前者数据在，清空输入是对的；后者数据不在，清空输入就等于把用户刚写的东西弄丢了。两者的视觉一致（都是红色内联提示），但行为必须不同。

失败态是这一页的关键：规范要求「不报告虚假的保存成功」，因此失败时既不能清空输入，也不能用会自动消失的 toast。

超过正文上限时，`maxlength` 由 `textarea` 原生阻止继续输入，同时计数器提示上限——**不得静默截断用户已写的内容**。

## 3. 灵感列表页 `pages/list`

Tab 第二项。

### 区块

| 区块 | 内容 |
| --- | --- |
| A 标题区 | 标题「我的灵感」，右上角**「选择」入口**（进入多选汇总）。**不写副标题**——排序是产品行为，不需要向用户报备实现方式 |
| B 列表 | 连续承载面 + 发丝分隔线，**不是一张张浮起的卡片** |
| C 底部 | 次要按钮「新建灵感」，跳转记录页；另有「已合并 N 条」入口（仅当存在已合并项时出现） |

### 列表项元素

| 元素 | 说明 |
| --- | --- |
| 正文首行 | 最多显示 2 行，超出用省略号。取 `text` 的首行 |
| 更新时间 | 相对时间（如「3 小时前」），取 `updatedAt`。分桶阈值见 `detailed-design.md` §7.8——**按经过时间分桶，不做日历日运算**，「昨天」按 24—48 小时计 |
| 补充条数 | 仅当 `supplements.length > 0` 时显示，形如「3 条补充」 |
| 照片数量 | 仅当 `photos.length > 0` 时显示 |
| 同步状态标记 | 仅在 `sync_failed` / `conflict` / `deleting` 时显示小标记。**不显示 `dirty`（待同步）**——那是一次保存动作的中间态，转瞬即逝，渲染出来就是把内部状态摊给用户 |

**不出现**热度分数或理由。**不出现**「已删除」的条目——处于 `deleting` 态的条目仍要显示并标注「删除中」，因为此时云端尚未确认。

### 状态

| 状态 | 呈现 |
| --- | --- |
| 空态 | 「还没有灵感」+「想到什么，就先记下来。」配一份文档线描图标 |
| 加载态 | 列表区显示「加载中…」占位；已有缓存数据时直接渲染缓存，不阻塞 |
| 失败态 | 内联提示「没能读到最新内容，先显示已经存下来的部分。」+ 「重试」按钮。**已缓存的内容继续显示**，不能因为刷新失败就清空 |

**选择态与汇总**（2026-09-22 新增，「AI 汇总」能力）：

- 标题区右上角「选择」进入多选态，每项前出现勾选框，底部浮出操作条。
- 勾选**两个以上**时「汇总」可用；只勾一个时置灰并说明「至少选两个」。**不得在数量不足时发起调用**——白耗额度。
- 汇总结果以草案面板呈现，要求用户**显式选择**「覆盖灵感」还是「一个新灵感」，**没有默认值**（见第 4 节 D）。
- 选「覆盖灵感」时，被汇总的灵感从列表**默认消失**；选「一个新灵感」时，被勾选的灵感**完全不动**。

**已合并灵感的入口**：

| 项 | 呈现 |
| --- | --- |
| 位置 | 列表末尾固定一行「已合并 N 条」，**不参与排序** |
| 展开 | 点进去查看被合并的灵感，逐条带「恢复」操作 |
| 恢复后 | 该灵感回到列表框，按它自己的 `updatedAt` 重新排序；汇总结果不受影响 |

**这一行必须有。** 已合并的灵感默认不出现在列表里，如果没有入口，它们就等于被删掉了——而规范明确要求它们可查看、可恢复。**默认隐藏 + 没有入口 = 删除**，这是这个设计里最容易踩空的一处。

排序始终按 `updatedAt` 倒序，最近有补充的排在最前。本机有新变更但未同步的条目，仍按本机的 `updatedAt` 排序——用户看到的顺序应符合他刚才的操作。

## 4. 灵感详情页 `pages/detail`

由列表页点击进入，`query.id` 传入灵感标识。骨架版本目前只显示 `id`，需整体重做。

### 区块

| 区块 | 内容 |
| --- | --- |
| A 原文区 | 正文可**直接编辑**；存在历史时出现「历史版本」入口；另有「补充」按钮 |
| B 补充时间线 | 按时间**正序**排列的补充条目 + 选择态 + 「追加补充」输入区；已合并的默认收起 |
| C 照片区 | 图片网格 + 「拍照」/「从相册选择」入口 |
| D AI 区 | 扩展入口 + 草案面板；选中两条以上补充时出现「汇总」入口（仅当 `ai.js` 的 `enabled` 为 true 时可调用） |
| E 删除 | 底部次要样式按钮「删除灵感」 |

### A 原文区

正文完整展示，不截断。**可以直接编辑**——点进去改，保存后生效。

> 2026-09-22 修订。初稿把这里做成只读，只给「我想补充」，依据是当时那条「原文不可改写」的规范。该约束已撤销：用户记下一句话、过了两天发现写偏了，他需要的是**把那句话改对**，而不是在下面追加一条「更正：上面那句应该写成……」。逼他用追加来绕，时间线会变成一叠补丁，而他真正想留下的那句话反而停在最初那个不准确的版本上。

改写保存后，原文区出现**「历史版本」入口**（仅在确实存在历史时出现），点开按时间倒序列出历次被替换掉的版本，各自标明被替换的时间。

- 历史条目**只读**，不提供「恢复到此版本」之外的编辑入口——它是记录，不是草稿。
- 历史**没有删除入口**。这是产品承诺「你说过的话不会被悄悄抹掉」的界面体现。
- 入口本身不强调，不给数字徽标——历史是备查的，不该每天提醒用户「你改过这句话 7 次」。

编辑正文时与其他文本输入同规则：清空不保存、超长拒绝且不截断、失败时保留输入并内联提示。

**历史版本页**（点「历史版本」进入）：

| 项 | 呈现 |
| --- | --- |
| 顺序 | 按被替换时间**倒序**，最近被换掉的在最上 |
| 每条 | 旧内容 + 「MM-DD HH:mm 被替换」。视觉上比当前原文**弱一档**（次要文字色、左侧细线） |
| 当前原文 | 固定在列表**下方**，用主色描边标出「当前原文」——让用户一眼看出哪个是现在的 |
| 操作 | **只读。没有编辑入口，也没有删除入口。** |

**没有删除入口是刻意的。** 这是产品承诺「你说过的话不会被悄悄抹掉」的界面体现——如果历史能删，那个承诺就只剩下前半句。

### B 补充时间线

每条补充显示：内容、记录时间、来源标记。`source` 为 `'ai'` 的条目**必须**带可见的「AI 生成」标记，且标注「内容可能不准确，请自行判断」。

追加补充输入区位于时间线下方，`maxlength` 取 `LIMITS.supplementMaxLength`。提交失败时保留输入并内联提示，与记录页同规则。

状态：

| 状态 | 呈现 |
| --- | --- |
| 无补充 | 时间线区显示「还没有补充」 |
| 提交中 | 按钮「提交中…」且 disabled |
| 提交失败 | 输入保留 + 内联失败提示 + 可重试 |
| 内容为空 | 按钮 disabled，不发请求。规范要求空补充不写入、也不改变更新时间 |

**选择态与汇总**（2026-09-22 新增，「AI 汇总」能力）：

- 时间线附近提供「选择」入口，进入后每条补充前出现勾选框。
- 选中**两条以上**时底部浮出「汇总」按钮；只选一条时按钮置灰并说明「至少选两条」。**不得在数量不足时发起调用**——那会白白消耗额度。
- 汇总结果以**草案面板**呈现，与 AI 扩展共用同一种视觉（虚线描边 + 浅底 + 「未写入」），并在面板内要求用户**显式选择**「覆盖补充」还是「新增补充」。**没有默认值**——这是整条链路上不可逆程度最高的一步，不能替用户预选。
- 选「覆盖补充」时，被汇总的补充在时间线上**默认收起**；选「新增补充」时，被汇总的补充**完全不动**，仍独立可见。

**已合并补充的呈现**：

| 项 | 呈现 |
| --- | --- |
| 收起态 | 一行摘要 +「展开」，位置留在时间线上原处。**两种收起的措辞必须不同**：被 AI 汇总的写「N 条已合并」，被合并进灵感的写「1 条已并入灵感」——只写「合并」两个字，用户分不清并到哪去了 |
| 展开态 | 逐条展示原内容与记录时间，每条带「恢复」操作 |
| 恢复后 | 该条回到时间线上的原位置（仍按时间排序），汇总结果与原文均不受影响，两者并存 |

**不能没有入口。** 已合并的内容只是默认收起，不是被隐藏。如果界面上没有任何地方能看到和恢复它们，那和删除没有区别——而规范明确要求它们可查看、可恢复。

**长按一条补充的操作面板**（2026-09-22 新增）：

| 选项 | 说明文字 | 后果 |
| --- | --- | --- |
| 修改 | —— | 进入编辑态，改完保存；旧内容进历史，可回看 |
| 合并进灵感 | 「内容会追加到原文末尾」 | 本条收起，可展开恢复 |
| 删除 | 「不可恢复」 | 再弹一次确认 |

视觉与交互约束：

- **面板标题标明操作对象**，用「这条补充 · 昨天 21:40」这样的定位，**不要直接把内容截断成一句引用**——那样引用句孤零零浮着，用户看不出它跟哪个操作有关联。
- **「删除」用危险色，且与另外两项在视觉上分开。** 不能和「修改」长得一样、紧挨着排——长按是容易误触的手势。
- **点「删除」必须再弹一次确认**，明确写出「此操作无法撤销」。一次点击就删掉用户写下的东西，是不可接受的。
- **确认弹窗要把待删内容放进一个带时间的框里**，正文写「这条补充将被移除，此操作无法撤销」，框里是内容本身与它的记录时间。这样「要删的就是这条」这件事才是连着的，而不是两句不相干的文字摆在一起。
- 「合并进灵感」旁要写清后果——用户需要知道这一步到底做了什么，而不是只看到一个词。
- 已合并或已并入灵感的补充，长按面板里**不提供「修改」**，只提供「恢复」与「删除」。它们已经不在时间线上正常显示，先恢复再改，用户才看得清自己在改什么。
- **「合并」在本产品里有两个用法，界面必须让目标可分辨**：长按面板的「合并进灵感」并入的是**灵感原文**；AI 汇总的「覆盖补充 / 覆盖灵感」并入的是**汇总结果**。不能只写「合并」两个字。完整对照见 `specs/inspiration-capture/spec.md`。

### C 照片区

网格布局，每张图带独立状态角标：

| 照片状态 | 角标呈现 |
| --- | --- |
| `uploading` | 「上传中…」遮罩 |
| `upload_failed` | 「上传失败，点击重试」+ 可点击 |
| `uploaded` | 无角标 |
| `rejected` | 不进入网格，以提示条说明「有 1 张图片超过 2 MB，已经跳过。」——只说事实，不补「其余不受影响」这类辩解 |

达到 `LIMITS.photosPerInspiration`（9 张）后，添加入口变为 disabled 并说明「最多 9 张」。

状态：空态显示「还没有照片」；权限被拒时**不读取任何图片**，显示内联说明与开启指引，且不影响本页其余区块。

### D AI 区

承载两个动作：**扩展**（把一句话展开成结构化草案）与**汇总**（把多条补充收成一条）。两者共用同一种草案面板视觉，不各自发明一套。

未启用时（`ai.js` 的 `enabled` 为 `false`）：显示说明文案「AI 扩展还没开放。」，**不展示**可点击的调用入口（汇总入口同样不出现），也不展示任何看起来像 AI 结果的本地内容。**不要补「记录、补充、拍照与浏览不受影响」**——那是规范条款，不是用户需要知道的事。

启用时，**扩展**的草案面板：

- 三个分区展示：要点 / 下一步 / 风险，每项前带采纳勾选框；
- **视觉上明确「尚未成为你的数据」**：虚线描边 + 浅底，左上角标注「未写入」；
- 顶部标注「由 AI 生成，可能不准确。勾选后才会写进这条灵感，没勾的会被丢弃。」；
- 每项支持行内修改后采纳；
- 底部两个操作：「采纳所选（N 条）」与「放弃」。

**汇总**的草案面板：

- 单段文本，不分区——汇总的产物是一条连续的内容，不是分析结果；
- 同样标注「未写入」与「由 AI 生成，可能不准确」；
- 额外要求**选择写入方式**：「覆盖补充」与「新增补充」，两个选项并列、**都不预选**；
- 底部操作：「确认」与「放弃」；
- 在「覆盖」选项旁用一句小字说明后果：「被合并的内容会收起，之后可以展开恢复。」——用户需要知道这一步到底做了什么，而不是只看到一个词。

草案面板的所有状态都是**页面内存态**：关闭、返回、杀进程后不保留，重新进入不得出现半成品。

### E 删除

点击后二次确认（`wx.showModal`），确认后进入 `deleting` 态：

- 页面顶部出现内联提示「删除中…」，条目保持可见；
- 成功后返回列表页并移除该条；
- 失败后**全部保留**，内联提示「删除失败，内容未做改动」+ 可重试。

不得在云端确认前从本机移除，否则会出现「本机已删、云端还在」的状态。

## 5. 我的页 `pages/mine`

Tab 第三项。骨架版本已有结构良好，按下表调整文案与区块即可。

### 区块

| 区块 | 内容 |
| --- | --- |
| A 能力状态 | 云端同步 / AI 扩展 两行状态，取 `app.globalData`。只显示状态，不加工程说明 |
| B 数据与隐私 | 数据存放说明 + 照片用途说明。**不写「只存在这台手机上」这类告警**，见本节「文案要求」 |
| C 反馈 | `open-type="feedback"` 按钮。区块标题就叫「反馈」，不写「使用帮助与反馈」这类填充词 |

### 文案要求

- **不区分本机存储与云端存储。** 2026-09-22 决定：保存后自动同步云端，失败则提示，界面不呈现「本机 / 云端」的二分。因此**不要**写「数据只存在这台手机上」这类告警——那是把实现分层摊给用户看。本机存储不可靠是工程事实（见 `detailed-design.md` §3.6），但它的对策是**让云端成为数据来源**，不是让用户去理解有两层存储。
- **清单上不出现「待同步」。** 保存后立刻同步，中间态转瞬即逝，不该被渲染成常驻标记。只有 `sync_failed` 与 `conflict` 才需要出现在界面上（见 `detailed-design.md` §5.1）。
- **照片用途**必须单独说明，但要用用户语言。原型采用：「照片只给你自己看，不会被分享，也不会用于训练。删除灵感时，云端上的照片会一起删掉。」规范里要求的「不写入日志」属于工程保证，用户读不懂，不写进界面。
- **能力状态区块不写工程说明。** 原型初稿曾写「两项均需先完成资源创建授权、部署与验收，不能通过直接改开关生效」——那是开发者的话，用户不该看到。只显示「未启用」即可。
- **不得出现热度相关文案**。也不得用「优先级评分」「推荐指数」等别的名字暗示存在评分功能——热度已暂缓，此处暗示即为不实陈述。

## 6. 文案基调与合规红线

### 6.1 基调

**短句，陈述事实。不推销、不解释架构、不辩解、不罗列功能。**

这个产品是一个人的私人灵感本，界面文案应该像一个安静好用的工具，而不是一个急着证明自己的 App。以下七类写法一律禁止：

| 禁止 | 反例（曾出现在原型里） | 问题 |
| --- | --- | --- |
| 功能罗列当卖点 | 「先写下来，之后可以随时补充、拍照，再让 AI 帮你展开」 | 像广告语；且在 AI 默认关闭时承诺 AI 能力，属不实描述 |
| 把内部实现讲给用户 | 「按最近更新时间倒序排列」 | 排序是产品行为，不需要向用户报备实现方式 |
| 把工程流程暴露给用户 | 「两项均需先完成资源创建授权、部署与验收，不能通过直接改开关生效」 | 这是开发者的话，用户不关心也不该看到 |
| 把规范条款抄成界面文案 | 「记录、补充、拍照与浏览不受影响」 | 功能矩阵不是文案；用户只会困惑为什么突然提这个 |
| 技术名词直出 | 「不上传至任何对外展示场景、不写入日志」 | 「写入日志」对用户没有意义，是工程保证而非用户语言 |
| 过度解释与辩解 | 「已跳过，其余图片不受影响」 | 后半句是在替自己开脱；把事实说清楚就够了 |
| 推销口吻 | 「让 AI 帮你展开」 | 与「不得夸大 AI 效果」的规范冲突 |
| **开发脚手架漏进产品** | 「工程骨架版本尚未实现」「详细范围见 openspec/…/proposal.md」「启用需先完成资源创建授权、部署与验收」 | 这些是写给开发者自己的，使用者看到只会觉得产品没做完、或者被内部流程打扰 |

最后一条单独强调：**开发期的进度说明、提案路径、授权流程、任务编号，一律不得出现在任何面向使用者的位置**，包括页面文案、按钮提示、toast、以及会进包的日志。它们属于代码注释，不属于产品。
（`console.info` 这类纯开发者日志可以保留诊断信息，但不要写「骨架版本」「尚未实现」这类带开发阶段判断的话。）

### 6.2 不要过于口语化（2026-09-22 补）

**「不推销」不等于「可以随便说」。** 界面是工具，不是聊天。按钮与操作标签要用规范的说法，口语化的措辞会让产品显得不严谨。已改过的一批：

| 原措辞 | 改为 |
| --- | --- |
| 去记一条 | 新建灵感 |
| 再补充一点… | 添加补充 |
| 删除这条灵感 | 删除灵感 |
| 删除后无法恢复 | 此操作无法撤销 |

判断标准：**这句话写进合同或说明文档里，读着会不会别扭。** 会，就是太口语。

### 6.3 写入方式的选项按上下文命名

汇总的两种写入方式不要用「覆盖原来的 / 存一个新的」这类含糊说法——「原来的」指什么，取决于上下文，用户读不出来。按操作对象直接命名：

| 场景 | 覆盖 | 另存 |
| --- | --- | --- |
| 补充汇总 | 覆盖补充 | 新增补充 |
| 灵感汇总 | 覆盖灵感 | 一个新灵感 |

两个选项**都不预选**，理由见第 4 节 D。

改写的方向：**只说用户此刻需要知道的那一件事**。列表顺序不用说，缓存机制不用说，功能是否互相影响不用说。

### 6.4 合规红线

以下措辞是规范硬性要求，不是风格偏好：

| 场景 | 必须出现 | 必须不出现 |
| --- | --- | --- |
| 保存失败 | 明确说明没有保存成功 | 任何暗示已保存的措辞 |
| AI 结果 | 「由 AI 生成」「可能不准确」 | 把本地规则结果说成 AI 生成 |
| AI 未启用 | 说明还没开放 | 任何看似 AI 产出的内容、任何调用入口 |
| AI 草案 | 说明确认前不写入 | 让用户以为草案已经是自己的数据 |
| 照片 | 说明仅自己可见、删除时同步清理 | 任何分享、公开、对外展示的入口 |
| 删除失败 | 说明内容未做改动 | 静默失败或提前移除 |
| 热度 | —— | 任何形式的评分、热度、优先级暗示 |

## 7. 骨架版本的开发痕迹清理（已完成 2026-09-22）

初始化骨架为了让页面在开发者工具里可预览，写了一批自述性的说明文字。这些文字全部属于第 6.1 节最后一条禁止的「开发脚手架漏进产品」，已清除：

| 位置 | 移除对象 | 现状 |
| --- | --- | --- |
| `pages/capture/index.wxml` | 末尾的 `.placeholder` 工程说明块 | 已删除；副标题改为「半句话也可以。」 |
| `pages/detail/index.wxml` | `.placeholder` 说明块与「灵感标识」调试卡片 | 已删除；该页待第 2 阶段实现 |
| `pages/list/index.wxml` | 空态文案中的「工程骨架版本尚未接入数据」表述，以及副标题「按最近更新时间倒序排列。」 | 已删除；空态改为「还没有灵感。想到什么，就先记下来。」 |
| `pages/mine/index.wxml` | 能力状态中的授权/部署/验收说明 | 已删除；区块标题「当前能力状态」→「能力」，「使用帮助与反馈」→「反馈」 |
| `pages/capture/index.js` | toast「骨架版本尚未实现保存」 | 改为「暂时还不能保存」——只陈述当前限制，不提开发进度 |
| `pages/mine/index.js` | `privacyNote` 中的「不会被写入日志、不会用于模型训练」 | 改为用户语言，见第 5 节文案要求 |
| `app.js` | `console.info` 中的「当前仅为工程骨架，业务功能尚未实现」 | 改为纯诊断信息「云能力未启用。」 |

代码注释中保留开发说明不受此限——注释是写给开发者读的，不进产品。
