<view class="page">
  <view class="title">我的</view>

  <view class="card">
    <view class="hint">当前能力状态</view>
    <view class="status-row">
      <text>云端同步</text>
      <text class="{{cloudEnabled ? 'on' : 'off'}}">{{cloudEnabled ? '已启用' : '未启用'}}</text>
    </view>
    <view class="status-row">
      <text>AI 扩展</text>
      <text class="{{aiEnabled ? 'on' : 'off'}}">{{aiEnabled ? '已启用' : '未启用'}}</text>
    </view>
    <view class="hint status-note">
      骨架版本两项均为未启用。启用需先完成资源创建授权、部署与验收，
      不能通过直接改开关生效。
    </view>
  </view>

  <view class="card">
    <view class="hint">数据与隐私</view>
    <view class="status-note">{{privacyNote}}</view>
  </view>

  <view class="card">
    <view class="hint">使用帮助与反馈</view>
    <button class="button-primary" open-type="feedback">提交反馈</button>
  </view>
</view>
