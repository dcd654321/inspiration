/* 全局样式与设计令牌。
 *
 * 令牌值来自 docs/ui-design.md 第 1 节，界面原型见 docs/ui-mockup.html。
 * 原型用的是 375px 视口下的 px，这里是 750rpx 设计稿下的 rpx——**换算关系是 ×2**。
 * 改这里的值之前先确认设计文档也跟着改了，否则真机与原型会对不上。
 */

page {
  /* 墨色由深到浅 */
  --ink: #16161d;
  --ink-2: #5f5f6e;
  --ink-3: #9b9baa;

  --paper: #f5f5f8;          /* 页面底色 */
  --surface: #ffffff;        /* 承载面 */
  --line: #e8e8ee;           /* 发丝分隔线 */
  --line-strong: #d8d8e2;

  --brand: #3d3d8f;
  --brand-soft: #ececf7;
  --brand-line: #c9c9e6;

  --danger: #b3382c;
  --danger-soft: #fbeeec;
  --danger-line: #efc9c4;

  --warn-ink: #8a6420;
  --warn-soft: #fdf4e3;

  background: var(--paper);
  color: var(--ink);
  font-size: 30rpx;
  line-height: 1.6;
  font-family: -apple-system, BlinkMacSystemFont, "Helvetica Neue", "PingFang SC", "Microsoft YaHei", sans-serif;
}

/* ---------------------------------------------------------------- 布局 */

.page {
  padding: 36rpx 40rpx 48rpx;
}

.page-title {
  font-size: 44rpx;
  font-weight: 600;
  letter-spacing: -0.01em;
  line-height: 1.35;
}

.page-sub {
  font-size: 26rpx;
  color: var(--ink-2);
  line-height: 1.7;
  margin-top: 10rpx;
}

/* 承载面：只在真正需要「一整块内容」的地方用，不滥用卡片把页面切成一格一格 */
.panel {
  background: var(--surface);
  border-radius: 24rpx;
}

.divider {
  height: 1rpx;
  background: var(--line);
}

.section-head {
  display: flex;
  align-items: baseline;
  justify-content: space-between;
  margin-bottom: 18rpx;
}

.section-head .k {
  font-size: 24rpx;
  font-weight: 600;
  letter-spacing: 0.02em;
  color: var(--ink-2);
}

.section-head .v {
  font-size: 24rpx;
  color: var(--ink-3);
}

/* ---------------------------------------------------------------- 按钮 */

.btn {
  font-size: 30rpx;
  font-weight: 500;
  letter-spacing: 0.01em;
  border: none;
  border-radius: 16rpx;
  height: 80rpx;               /* 40px，不小于 44px 的触摸目标由 padding 补足 */
  padding: 0 44rpx;
  background: var(--brand);
  color: #ffffff;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  line-height: 1;
}

.btn::after { border: none; }  /* 去掉小程序 button 默认的边框 */

.btn-block {
  display: flex;
  width: 100%;
}

.btn[disabled],
.btn.is-disabled {
  background: #e4e4ec;
  color: var(--ink-3);
}

.btn-quiet {
  background: transparent;
  color: var(--brand);
  border: 2rpx solid var(--brand-line);
}

.btn-plain {
  background: var(--surface);
  color: var(--ink-2);
  border: 2rpx solid var(--line-strong);
}

.btn-danger {
  background: transparent;
  color: var(--danger);
  border: 2rpx solid var(--danger-line);
}

.btn-sm {
  height: 68rpx;
  font-size: 27rpx;
  padding: 0 32rpx;
  border-radius: 14rpx;
}

/* ---------------------------------------------------------------- 提示条 */

/* 关键失败一律用页面内联，不用会消失的 toast——用户错过之后会以为已经成功 */
.notice {
  display: flex;
  align-items: flex-start;
  margin-top: 24rpx;
  padding: 22rpx 26rpx;
  border-radius: 16rpx;
  font-size: 26rpx;
  line-height: 1.6;
}

.notice .mark {
  flex: 0 0 auto;
  width: 30rpx;
  height: 30rpx;
  border-radius: 50%;
  margin: 4rpx 18rpx 0 0;
  position: relative;
}

.notice .mark::before {
  content: "";
  position: absolute;
  left: 13rpx;
  top: 8rpx;
  width: 4rpx;
  height: 11rpx;
  background: #ffffff;
  border-radius: 2rpx;
}

.notice .mark::after {
  content: "";
  position: absolute;
  left: 13rpx;
  bottom: 7rpx;
  width: 4rpx;
  height: 4rpx;
  background: #ffffff;
  border-radius: 50%;
}

.notice-err {
  background: var(--danger-soft);
  border: 2rpx solid var(--danger-line);
  color: var(--danger);
}
.notice-err .mark { background: var(--danger); }

.notice-warn {
  background: var(--warn-soft);
  color: var(--warn-ink);
}
.notice-warn .mark { background: var(--warn-ink); }

/* ---------------------------------------------------------------- 标签 */

.tag {
  font-size: 22rpx;
  line-height: 1;
  padding: 8rpx 16rpx;
  border-radius: 40rpx;
  letter-spacing: 0.01em;
}

.tag-ai { background: var(--brand-soft); color: var(--brand); }
.tag-fail { background: var(--danger-soft); color: var(--danger); }

/* ---------------------------------------------------------------- 空态 */

.empty {
  background: var(--surface);
  border-radius: 24rpx;
  padding: 92rpx 56rpx 88rpx;
  text-align: center;
}

.empty .et {
  font-size: 30rpx;
  font-weight: 600;
  letter-spacing: -0.01em;
  color: var(--ink);
}

.empty .ed {
  font-size: 26rpx;
  color: var(--ink-2);
  line-height: 1.7;
  margin-top: 12rpx;
}

/* ---------------------------------------------------------------- 列表 */

.list {
  background: var(--surface);
  border-radius: 24rpx;
  overflow: hidden;
}

.row {
  padding: 30rpx 32rpx 28rpx;
  border-bottom: 2rpx solid var(--line);
}

.row:last-child { border-bottom: none; }

.row .body {
  font-size: 31rpx;
  line-height: 1.68;
  letter-spacing: 0.01em;
  display: -webkit-box;
  -webkit-line-clamp: 2;
  -webkit-box-orient: vertical;
  overflow: hidden;
}

.row .meta {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  margin-top: 18rpx;
  font-size: 24rpx;
  color: var(--ink-3);
}

.row .meta .sep {
  width: 2rpx;
  height: 18rpx;
  background: var(--line-strong);
  margin: 0 20rpx;
}
